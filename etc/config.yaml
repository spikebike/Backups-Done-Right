
client:
        PrivateKey: ../certs/client.key
        PublicKey:  ../certs/client.pem
        BackupDirs: 
        - test
        Threads: 4
        SqlFile: etc/fsmeta.sql
        QueueBlobDir: /tmp/bdr
        ExcludeDirs: .git


